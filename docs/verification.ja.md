# 検証記録

[English](verification.md) | 日本語

今回の更新の確認環境：2026年9月18日、Node.js 22.16.0、Python 3.13.5。

## 確認済み

- Nodeテスト28件、Pythonテスト9件：すべて成功。API応答はモックを使用。
- 保存結果1,941件のオフライン再集計。
- 計画1,942件の入力再構成。既知の欠落はNoulの `shard6-186`。
- npm依存パッケージを導入せず、3種類のサンプルをAPIキー・通信なしでプレビュー。
- ドキュメントの相対リンク、日英の相互リンク、MIT表記の整合性。
- 保存データ、入力スナップショット、数値集計、サンプル、図が前版とバイト一致。

変更はドキュメント、ライセンス表記、生成するMarkdown集計の言語です。数値計算と実験入力は変えていません。既存の図はそのまま保持し、今回の更新では再描画していません。

## 未確認

新しいモデルAPI呼び出しは行っていません。前版の作成環境では通信制約によりSDKを導入できず、今回も実SDKでの統合動作確認は追加していません。依存パッケージの版、取得先URL、integrityは変更していません。

GitHub Actionsは、オフラインテスト、固定SDKのインストール、`experimental_evaluate` のexport確認を行う設定です。Actions自体はこの環境では実行していません。

過去のサーバー応答の真正性、モデル内部の実装、現在のモデルが同じ出力を返すことまでは確認していません。詳細は[方法と限界](methods.ja.md)と[実行ガイド](running.ja.md)を参照してください。
